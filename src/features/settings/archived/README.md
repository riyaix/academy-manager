# Archived settings UI

Typography, theme (light/dark/system), and visual size controls used to live under
**System preferences**. They are intentionally hidden from the Settings screen —
not deleted — so they can be restored later without rediscovering the UX.

Language and backup now live under **Organization & appearance**.
