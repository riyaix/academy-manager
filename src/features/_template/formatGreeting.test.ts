/**
 * Example domain test for `formatGreeting`.
 * Uncomment when Vitest is configured (see docs/product/PLAN.md §6.1).
 *
 * import { describe, expect, it } from "vitest";
 * import { formatGreeting } from "./formatGreeting";
 *
 * describe("formatGreeting", () => {
 *   it("returns a generic greeting when name is empty", () => {
 *     expect(formatGreeting("  ")).toBe("Hello!");
 *   });
 *
 *   it("includes the trimmed name", () => {
 *     expect(formatGreeting(" Ada ")).toBe("Hello, Ada!");
 *   });
 * });
 */

export {};
